<% if current_customer.following?(customer)  && customer.name != "guest_user" %>
  <%= link_to "フォロー外す",customer_relationship_path(customer.id,current_customer),remote: true,class:"btn btn-outline-danger", method: :delete %>
<% elsif current_customer.name != "guest_user" %>
  <%= link_to "フォローする", customer_relationships_path(customer.id),remote: true,class:"btn btn-outline-primary", method: :post %>
<% end %>